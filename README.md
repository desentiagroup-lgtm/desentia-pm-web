# DeSentia PM Web (React + Vite)

This is a tiny starter to deploy on **Cloudflare Pages** (free).

## Quick start
```bash
npm install
npm run dev
```
Build for production:
```bash
npm run build
```
The production output directory is `dist/`.
